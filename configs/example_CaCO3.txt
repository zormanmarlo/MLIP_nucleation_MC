# CONCENTRATION
concentration = 0.075
num_particles = 600  # Total atoms: 120 Ca + 120 CO3 (each CO3 = 4 atoms) = 120 + 480 = 600

# SIMULATION SETTINGS
equil_steps = 100
prod_steps = 400000
output_interval = 100
internal_interval = 1000
seed = 11

# BIAS SETTINGS
bias_type = none

# MOVE RATES (must sum to reasonable values)
avbmc_rate = 0.2
nvt_rate = 0.1
translation_rate = 0.5
rotation_rate = 0.2
swap_rate = 0.0
max_displacement = 30

# CLUSTER DEFINITION
upper_cutoff = 3.5
lower_cutoff = 2.5
clust_cutoff = 3.5

# INPUT FILES
model_path = /path/to/your/nequip/model.pth
input_path = none
debug_mode = False

# MOLECULAR GEOMETRY (optional - defaults to standard CO3 if not specified)
# body_file = CO3.body

# Optional rotation parameter
# max_rotation_angle = 3.14159  # radians, default is pi
